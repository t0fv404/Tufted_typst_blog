/**
 * User-defined site-wide scripts.
 *
 * This file is loaded on every page. Add site-wide behavior here; the build
 * copies it to _site/js/custom.js automatically.
 */
(() => {
	// Add custom JavaScript here.
})();
